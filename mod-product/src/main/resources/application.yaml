server:
  port: 8802
spring:
  application:
    name: VP-PRODUCT
  datasource:
    url: jdbc:postgresql://localhost:5433/hitech
    username: phuocvh
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
      show-sql: true